apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: civicpulse-dev

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: backend
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/envFrom/0/configMapRef/name
        value: civicpulse-config
